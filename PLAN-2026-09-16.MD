# 2026-09-16 日清夜跑输入材料

本文件对应 GitHub 仓库 `https://github.com/zc125/PLAN.git` 的目标路径：
`daily/2026-09-16/PLAN.MD`

## 一、任务背景

今晚仅围绕 Gumption-Trap 船体项目的 3D 建模装配模型，开展“输入确认 -> 干态检查 -> 低风险多环境模拟 -> 结果归档”的夜跑。

已知基础：已有推进仿真工作台和 2026-09-04 的多工况历史结果；尚未发现可直接读取的原生 3D CAD 装配工程或有效的本轮装配运行日志。历史水动力结果仅可作为趋势基线，不能代替当前装配模型或实物结果。

## 二、任务目标

1. 确认当前可用模型及其版本、单位、坐标系、零件层级和装配基准。
2. 对可用模型进行静态干涉、安装、维护和线束空间检查。
3. 在输入可追溯的前提下，执行低风险推进与电源边界场景，并保留完整参数和日志。
4. 输出装配修改项、参数缺口、实物验证项和阻塞项。

## 三、输入资料

- 3D CAD 原生装配、可导入网格或装配快照；
- 船体、底板、舱盖、Teensy、GPS、LIS2MDL、LCD、ESC、T200、电池、连接器、紧固件与线束资料；
- `仿真工作台/太阳能自主船推进仿真工作台.html`；
- `outputs/电机水中环境仿真_2026-09-04/夜跑任务执行结果/` 下的历史报告和 CSV；
- 重量/重心、浮力/阻力、推进器位置与浸没、电池、ESC、材料、密封和载荷参数。

## 四、执行任务

### 任务 1：模型输入门禁

记录每个模型文件的路径、版本、格式、单位、坐标系、原点、零件层级、装配约束和来源。缺少其中任一关键项时，标记“输入缺失/阻塞”，不开展几何或载荷求解。

### 任务 2：干态装配检查

检查船体/底板/舱盖闭合，安装孔与紧固件、连接器插拔、线束弯曲半径、旋转件避让、传感器位置、推进器轴线、维护工具通道和密封界面。保存碰撞列表、最小间隙、不可达件和待修订位置。

### 任务 3：参数映射

将装配对象映射到仿真参数：左右推进器位置与方向、浸没深度、船体质量/重心、阻力、正常电压、内阻、ESC 保护阈值。每个参数记录来源、单位和证据等级；未确认值不得伪装为实测值。

### 任务 4：低风险场景

仅在任务 1-3 通过后运行：干态静态、静水双机同速、低幅度左右差速和快速换向保护。每个场景保留输入快照、起止时间、求解状态、异常日志、有效时间窗口和输出摘要。

### 任务 5：扩展边界场景

依次运行同向水流、逆向水流、回流/碰壁、低电压和高内阻。碰壁后速度数据不得进入性能评价；掉压、过流、保护或发散必须单列为风险结果。

### 任务 6：机械与环境风险

只有材料、紧固、密封及外部载荷有可靠来源时，才进行振动、冲击、温度和进水路径的定量求解。否则输出风险位置和资料缺口，不生成应力、温升、寿命或防护等级数值。

## 五、执行方式与输出

1. 每个场景单独保存参数文件、日志、结果摘要和证据等级。
2. 结果按“模型检查通过”“需修改装配”“待实物验证”“输入缺失/阻塞”归类。
3. 输出至少包括：模型输入清单、装配干涉/维护清单、场景矩阵、异常/停止清单、结论报告。
4. 不覆盖历史结果；所有新的结果都要标明装配版本和参数来源。

## 六、停止条件

- 模型版本、单位、坐标系、装配约束或关键几何缺失；
- 检测到实体穿透、约束丢失、无法解释的零件位置或线束穿越运动件；
- 求解不收敛，或输入/输出超出定义边界；
- 低电压、过流、保护未触发、输出未回安全状态或控制误差发散；
- 需要以假设替代材料、密封、尺寸、载荷或真实硬件性能。

## 七、预期产出与用途

形成可追溯的 3D 装配输入与低风险多环境验证记录，明确模型可用程度、须修订的装配位置、需要补充的参数和下一轮实物验证顺序。模型输出仅指导设计与测试准备，不能声明真实水域、密封、热、振动或可靠性已验证。

## 八、记录标识

2026-09-16|张灿|日清
