# 2026-09-01 日清夜跑输入材料

## 一、任务背景

今晚的核心任务是使用 Teensy 4.1 完成 GPS 和 LIS2MDL 磁力计的编程，并实现两类传感器的基础联调。目标是先打通数据采集链路，不进入自动航点和推进器控制。

## 二、今日实际基础

1. Arduino IDE `2.3.10` 已安装。
2. Teensy Boards `1.62.0` 已安装，目标板卡为 `teensy:avr:teensy41`。
3. Rosetta 2 已安装。
4. Teensy 4.1 Blink 示例已成功编译并生成 `.hex`、`.elf` 等产物。
5. 已确认 GPS 与 LIS2MDL 的功能分工：GPS 提供位置数据，LIS2MDL 提供磁场和基础航向数据。

## 三、今晚输入给 AI 的任务

1. 在现有 `teensy41_gumption_trap` 工程中完善 GPS 程序：使用 `Serial1` 接收 NMEA，解析有效定位、纬度、经度、卫星数和海拔，并周期性输出。
2. 完善 LIS2MDL 程序：使用 I2C 读取 X/Y/Z 磁场数据，计算未校准基础航向角，并周期性输出。
3. 将 GPS 和磁力计放入同一主循环，保证两类数据能够同时更新和输出。
4. 使用 `teensy:avr:teensy41` 编译工程，记录编译结果、缺少库和必要修复。
5. 如果 Teensy、GPS 和 LIS2MDL 已连接，继续进行烧录和串口联调；如果未连接，只记录代码和编译验证，不虚构硬件结果。

## 四、接线前提

- GPS TX -> Teensy Pin 0（Serial1 RX）。
- GPS RX -> Teensy Pin 1（Serial1 TX）。
- GPS UART 电平必须确认是 3.3V TTL。
- LIS2MDL SDA -> Teensy Pin 18。
- LIS2MDL SCL -> Teensy Pin 19。
- GPS、LIS2MDL 和 Teensy 必须共地。
- LIS2MDL 航向暂按未校准结果使用，只验证数据变化。

## 五、执行方式

先检查现有模块代码和库依赖，再完成最小代码修改；随后编译；有实物时按 GPS、LIS2MDL、两者同时运行的顺序联调。所有输出应明确区分“代码编译通过”和“真实传感器读取通过”。

## 六、约束条件

- 今晚只做 GPS + LIS2MDL 基础联调。
- 不扩展自动航点、推进器控制、声呐、太阳能和电源监测。
- 未确认电平和共地前，不给传感器上电或直连 Teensy。
- 不把未连接硬件时的程序编译结果写成传感器实测结果。

## 七、停止条件

- GPS 程序已接入并能编译；
- LIS2MDL 程序已接入并能编译；
- 已形成两类数据同时输出的基础联调结果；
- 或因缺少实物、库、接线或端口而阻塞，并已记录具体原因。

## 八、预期结果与用途

产出 Teensy 4.1 上的 GPS + LIS2MDL 基础联调程序、编译结果、串口输出格式和待实物验证清单，为后续自动航向和自动航点控制提供可靠的传感器数据基础。
