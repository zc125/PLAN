# 2026-09-07 船体项目复刻日清夜跑输入材料

## 一、任务目标

围绕船体项目复刻，完成 Teensy 4.1、GPS、磁力计和推进控制数据链路的联调准备。今晚重点验证数据是否能够稳定采集、正确输出、统一记录，并为后续双 ESC、推进器和船体水面测试建立顺序。

当前工作属于代码、串口和数据逻辑联调，不等同于真实船体下水或完整航行测试。

## 二、复刻系统范围

本次只围绕船体项目复刻，系统范围包括：

1. 船体结构和安装方向。
2. Teensy 4.1 控制器。
3. GPS 定位模块。
4. 磁力计航向模块。
5. 左右推进器及双 ESC。
6. 电池和供电系统。
7. USB 串口日志和数据分析。

## 三、当前已知接口和输出口径

1. GPS 通过 Teensy 4.1 的 `Serial1` 接入。
2. 磁力计通过 I2C 接入。
3. 电脑端通过 USB Serial 观察控制器输出。
4. 电脑端串口观察参数为 115200。
5. 输出保留状态键值，并增加 `[GPS]`、`[MAG]` 等可读标签。
6. 需要统一记录时间戳、GPS 定位状态、经纬度、磁力计三轴数据、航向角、左右推进器指令和异常状态。

## 四、今晚给 AI 的执行任务

### 任务 1：整理数据链路

输出船体复刻系统的数据流和字段表：

- 时间戳。
- GPS 定位状态。
- 纬度、经度和卫星信息。
- 磁力计 X/Y/Z。
- 航向角。
- 左右推进器目标指令。
- 左右推进器实际状态。
- 电池电压和电流记录位置。
- 异常状态和停止原因。

### 任务 2：设计串口验证方法

检查以下情况：

1. GPS 无数据。
2. GPS 有 NMEA 数据但没有有效定位。
3. GPS 定位后经纬度不更新。
4. 磁力计无响应。
5. 磁力计数据恒定。
6. 磁力计数据跳变。
7. 航向角随船体转动变化方向相反。
8. 串口输出频率过高影响控制循环。

对每种情况给出判断依据、记录字段和下一步处理动作。

### 任务 3：模拟船体控制场景

至少模拟以下控制过程：

1. 停止：左右推进器指令为零，航向数据保持可读。
2. 直行：左右推进器指令相同，检查航向变化是否符合预期。
3. 左转：左右推进器形成差速，检查航向变化方向。
4. 右转：左右推进器形成相反差速，检查航向变化方向。
5. 航向修正：根据磁力计航向误差调整左右推进器指令。
6. GPS 暂时失效：控制器保持安全状态，不使用无效定位数据做错误修正。

模拟结果只用于验证控制逻辑和记录格式，不得写成真实船体运动结果。

### 任务 4：制定真实测试前顺序

按以下顺序输出检查卡：

1. 确认船体前后左右方向和传感器安装方向。
2. 确认 GPS、磁力计和控制器的接线。
3. 上电后只观察传感器数据，不启动推进器。
4. 空载检查左右推进器控制方向和 ESC 中立状态。
5. 低输出、短时间启动单侧推进器。
6. 低输出、短时间启动双侧推进器。
7. 进行固定船体条件下的左右转向测试。
8. 条件满足后再进行短时水面测试。

每一步都要给出开始条件、观察项、停止条件和记录内容。

### 任务 5：建立异常分类

至少覆盖：

- 无 GPS 定位。
- 磁力计航向异常。
- 传感器数据中断。
- 左右控制方向相反。
- 双侧推进器输出不一致。
- 电池电压异常。
- ESC 无法进入中立。
- 串口日志丢失或时间戳错乱。
- 船体安装方向和软件坐标定义不一致。

## 五、预期输出文件

1. `船体复刻数据字段表-2026-09-07.csv`
2. `GPS与磁力计串口验证记录-2026-09-07.md`
3. `船体推进控制场景模拟记录-2026-09-07.md`
4. `真实船体测试前检查卡-2026-09-07.md`
5. `船体复刻异常分类与处理-2026-09-07.md`
6. `夜跑结果报告-2026-09-07.md`

## 六、停止条件

满足以下任一条件即可结束今晚夜跑：

1. GPS、磁力计和推进控制的数据字段已经统一。
2. 传感器无效、航向异常和控制方向错误均有判断方法。
3. 停止、直行、左转、右转和航向修正场景已经完成逻辑模拟。
4. 已形成进入真实船体测试前的分级检查顺序。
5. 发现无法解释的数据时，已保留日志并停止继续提高推进输出。

## 七、需要协调与帮助

1. GPS 和磁力计准确型号、数据手册及安装位置。
2. 船体方向、推进器左右定义和 ESC 通道对应关系。
3. 串口日志保存方式和数据分析工具。
4. 双 ESC、推进器和电池的低输出测试条件。
5. 水面测试场地、固定/回收方式和紧急断电方案。

## 八、预期结果与用途

得到一套面向船体项目复刻的联调输入材料，用于：

1. 验证 GPS、磁力计和控制器的数据链路。
2. 统一船体复刻的遥测字段和异常判断标准。
3. 在真实推进测试前发现传感器、坐标方向和控制方向问题。
4. 支持后续双 ESC、推进器和船体水面测试。

## 九、记录标识

2026-09-07|待补充员工OpenID|日清
