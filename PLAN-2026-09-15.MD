# 2026-09-15 日清夜跑输入材料

本文件对应 GitHub 仓库 `https://github.com/zc125/PLAN.git` 的目标路径：
`daily/2026-09-15/PLAN.MD`

## 一、任务背景

今晚只围绕当前船体项目的 3D 建模与装配模型开展多环境模拟验证。用户已明确夜跑计划的核心是：对已经建模并组装的模型，在各个环境中进行模拟。

当前可用基线包括：

- 太阳能自主船推进仿真工作台；
- 双 T200、双 ESC、Teensy 4.1 的历史水中环境仿真结果；
- 船体、底板、GPS、LIS2MDL、ST7735 LCD、ESC、推进器、电池和线束的既有接口定义。

本夜跑的第一原则是区分模型结果和实物结果。没有原生 3D 装配、可靠参数或对应求解器时，只记录检查结果、趋势和阻塞，不伪造尺寸、应力、温升、密封等级、推力或可靠性数据。

## 二、验证目标

1. 证明 3D 装配模型的零件层级、单位、坐标系和安装基准可追溯。
2. 找出静态几何干涉、安装孔错位、连接器不可插拔、线束穿越运动件、维护空间不足和推进器轴线/浸没关系异常。
3. 在统一模型和参数下比较干态、水环境、电源边界、机械载荷和环境耐受场景。
4. 为每个场景定义输入、观测量、通过条件、停止条件、证据等级和实物验证需求。

## 三、输入资料与证据规则

### 资料

- 3D CAD 原生装配文件（若可用）；
- 装配清单、零件版本和接口基线；
- `仿真工作台/太阳能自主船推进仿真工作台.html`；
- `outputs/电机水中环境仿真_2026-09-04/夜跑任务执行结果/` 下的历史 CSV 和报告；
- Teensy、GPS、LIS2MDL、LCD、ESC、T200 和电池的规格/实测参数。

### 证据等级

- A：原生模型、明确参数和可复现运行日志共同支持；
- B：模型和部分参数支持，可用于设计趋势；
- C：假设或缺少关键参数，只能列为待确认；
- D：无输入或求解失败，标记阻塞。

## 四、模型准备与装配检查

1. 记录模型文件名、版本、单位、坐标系、原点和装配基准。
2. 列出船体、底板、舱盖、Teensy、GPS、LIS2MDL、LCD、ESC、T200、电池、连接器、紧固件和线束对象。
3. 检查固定方式、安装孔、螺钉/排针长度、连接器朝向、插拔方向和维护通道。
4. 检查静态碰撞体和包络：器件与壳体、底板与舱盖、线束与旋转件、推进器与船体/水面/容器壁。
5. 检查重心、浮力参考点、推进器轴线、浸没深度和左右对称基准；缺少质量或浮力数据时只做几何检查。
6. 保存模型快照和检查清单，再开始环境仿真；任何几何修改后重新检查。

## 五、多环境模拟矩阵

### 环境 A：干态装配与维护

检查舱盖闭合、器件间隙、连接器可达性、线束弯曲半径、螺钉工具空间、板边/壳体干涉和拆装路径。通过条件是无未解释的实体穿透，关键接口可插拔，维护件有明确工具通道。发现穿透、不可装配或维护件被遮挡时停止该场景。

### 环境 B：静水低风险推进

复用历史基线，先跑双机同速、单机和低输出阶梯。记录推进器浸没、速度、电流、电压、推力差、姿态趋势和控制器状态。只作为水动力模型基线，不宣称真实下水性能。

### 环境 C：流场与容器边界

覆盖同向水流、逆向水流、回流/碰壁和不同水深。记录回流影响、速度变化、左右推力差、壁面距离敏感性和结果收敛性。若容器尺寸或流场参数不明确，降级为 C 级趋势结果。

### 环境 D：差速与自动控制

覆盖左高右低、左低右高、快速换向和自动航向保持。检查左右通道映射、方向、切换过程、误差收敛和保护逻辑。出现输出未回中、误差发散或保护未触发时立即停止。

### 环境 E：电源边界

覆盖正常电压、低电压、高内阻和高输出风险。记录最低电压、峰值电流、掉压趋势、Teensy 重启、ESC 保护和安全输出。超过模型额定范围或出现数值发散时停止，不映射到真实电池测试。

### 环境 F：机械振动与冲击

对装配体施加有来源的预设载荷，检查底板固定件、推进器支架、连接器、线束和传感器安装点。没有材料、紧固预紧力或实测载荷时，不输出应力/寿命数值，只形成风险位置清单。

### 环境 G：温度与进水路径

以已知温度边界检查材料/密封界面变化，以几何路径检查舱盖、线束出口、连接器和排水路径。缺少材料热参数、密封件数据或 IP 试验条件时，只标记待实物验证，不生成防护等级结论。

## 六、执行方式

1. 先读取和校验模型，再按 A 到 G 的顺序运行；每个场景使用独立输入快照。
2. 记录输入参数、模型版本、运行时间、求解状态、异常日志、输出文件和证据等级。
3. 每个场景完成后输出：通过项、失败项、待确认项、停止原因和下一步动作。
4. 汇总为四类：模型检查通过、需修改装配、待实物验证、输入缺失/阻塞。
5. 结果文件至少包括装配干涉清单、多环境矩阵、异常问题清单和结论报告；不覆盖历史结果。

## 七、停止条件

立即停止当前场景并保留日志的条件：

- 原生模型、单位、坐标系或关键参数无法确认；
- 几何穿透、装配约束丢失或结果不收敛；
- 电流、电压、位移、温度或载荷超出模型边界；
- 保护逻辑未触发、输出未回安全状态或控制误差发散；
- 需要用未经确认的尺寸、材料、密封或实物性能补齐结论。

全局停止条件：缺少关键输入工程，或继续运行会诱导把模型仿真当成真实硬件/真实水域结论。

## 八、预期产出

1. 3D 装配干涉与维护空间检查清单。
2. 干态、水环境、电源边界、机械载荷和温度/进水场景的结果索引。
3. 每个环境的参数、通过条件、异常、证据等级和限制。
4. 需修改的模型位置、需补充的工程资料和实物验证顺序。
5. 一份可直接回填日清夜跑表的结论摘要。

## 九、记录标识

2026-09-15|张灿|日清
