# 2026-09-10 日清夜跑输入材料

## 一、任务背景

本次夜跑只围绕 Gumption-Trap 船体项目复刻的嘉立创EDA底板设计，不涉及其他项目。

当前船体复刻的已知接口基础：

1. Teensy 4.1 为主控。
2. GPS 通过 `Serial1` 接入，GPS TX 连接 Teensy Pin 0，GPS RX 连接 Teensy Pin 1；GPS 串口电平必须确认兼容 3.3 V TTL。
3. LIS2MDL 通过 I2C 接入，SDA/SCL 使用 Teensy Pin 18/19。
4. ST7735 LCD 使用硬件 SPI，SCK/MOSI 使用 Teensy Pin 13/11，CS/DC/RES 使用 Pin 6/14/15，BL 使用 Pin 16。
5. 左右 ESC PWM 当前规划使用 Teensy Pin 9/10，实际电调信号、电源和急停必须以最终设计资料为准。
6. 当前 GPS 串口和 LIS2MDL 基础读取已经验证过，但 GPS 室内仍可能无有效定位，磁力计仍未完成最终安装方向和校准。

今晚不把“嘉立创EDA DRC 通过”写成“底板已经能正常使用”。本次要分别验证：

- 原理图和网络连接是否正确；
- PCB 布局和制造规则是否合格；
- 电源、接口和信号逻辑是否满足设计假设；
- 哪些行为仍然只能通过打样板上电和外设联调确认。

## 二、底板验证范围

### 1. 电源模块

- 输入电源入口、保险、总开关/急停和反接保护。
- VIN 到 5 V 的转换。
- 5 V 到 3.3 V 的转换。
- Teensy、GPS、LIS2MDL、LCD 和接口负载的供电分配。
- 去耦、电源测试点、地回路、稳压器输入输出电容和散热。
- 电源负载、电压裕量、稳压器压差和异常断电状态。

### 2. 主控与调试

- Teensy 4.1 插座或排针的引脚编号、方向和机械定位。
- USB/烧录接口、复位、调试串口和测试点。
- 3.3 V GPIO 不得接入未经确认的 5 V 信号。
- 上电默认状态必须保持安全，不能让 ESC 输出误动作。

### 3. 传感器和显示

- GPS `Serial1` TX/RX、供电和共地。
- LIS2MDL I2C SDA/SCL、上拉、电源和地址。
- ST7735 SPI SCK/MOSI、CS/DC/RES/BL，确认 LCD 的 `SCL/SDA` 在本设计中不是 I2C。
- GPS 天线与磁力计、ESC、大电流线和金属件的空间隔离。
- LCD 背光电流和接口保护。

### 4. 推进控制接口

- 左右 ESC PWM 信号、信号地、通道编号和软件左右定义。
- ESC 中立值和上电默认安全值；当前项目历史约定为 1500 us，但必须以最终 ESC 型号和资料复核。
- 推进动力回路与逻辑控制回路分层，不通过逻辑 PCB 仿真替代真实高电流测试。
- 急停、人工接管和失效保护。

## 三、执行前的环境检查

如果使用 EasyEDA Agent 或本地 EasyEDA CLI，必须按以下顺序：

1. 先运行 `easyeda health`，确认 CLI、daemon 和 EasyEDA Pro 连接器版本匹配。
2. 使用 `easyeda doc ls` 或等效入口确认原理图、PCB 和工程名称。
3. 读取当前 Board 绑定关系，确认原理图和 PCB 属于同一块板。
4. 在任何放置、移动、布线、同步或删除前，先读取页面、器件、引脚、网络、层数、板框和 DRC 规则。
5. 如果没有原生 EasyEDA 工程、没有原理图或没有 PCB，停止实际修改，输出“缺少输入工程”的阻塞记录，不自行猜测设计。
6. 若 PCB 发生布线、过孔、铜皮或禁布区修改，先保存，再执行 `easyeda doc reload`，之后重新读取并检查。
7. 每个阶段通过后显式保存，并记录保存结果。

当前工作区未发现可直接打开的嘉立创EDA原理图/PCB原生工程文件，且本机未发现可调用的 `easyeda` 命令。因此如果用户没有在 EasyEDA Pro 中打开工程，今晚只能完成验证方案、接口对账模板和检查矩阵，不能声称已经完成真实 PCB 仿真。

## 四、任务 1：建立底板设计方案和接口对账表

输出底板模块清单：

1. POWER：输入、保险、保护、5 V、3.3 V、GND。
2. MCU：Teensy 4.1、复位、USB/烧录、调试。
3. GPS：UART、供电、天线区域。
4. MAG：LIS2MDL I2C、上拉、地址和去耦。
5. LCD：ST7735 SPI、背光、接口方向。
6. ESC_IO：左右 PWM、信号地、默认安全状态。
7. TEST：电源、地、UART、I2C、SPI、PWM 测试点。
8. MECH：安装孔、板框、连接器边缘位置和固定方向。

接口对账表至少包含：

- 模块；
- 原理图页；
- 器件位号；
- 芯片/连接器引脚；
- PCB 封装焊盘；
- 网络名；
- 目标 Teensy 引脚；
- 输入/输出方向；
- 逻辑电平；
- 供电电压；
- 是否共地；
- 是否需要上拉/下拉；
- 是否有测试点；
- 是否已与 PCB 同步；
- 证据来源；
- 结论；
- 待确认事项。

重点检查：

1. GPS TX/RX 是否交叉且没有反接。
2. LIS2MDL SDA/SCL 是否没有被接到 LCD 的 SPI SCK/MOSI。
3. LCD CS/DC/RES 是否各自独立，BL 是否经过合理驱动。
4. ESC PWM 左右通道是否与软件定义一致。
5. 3.3 V GPIO 是否可能受到 5 V 外设直接驱动。
6. 多个 GND/VCC 引脚是否全部真正连入对应网络，而不是只重叠或只写同名标签。

## 五、任务 2：电源和接口电气仿真

### A. 电源树

检查并尽可能仿真：

1. 输入电压的正常、最低和最高工作点。
2. 5 V 稳压输出、电流容量、压差和纹波假设。
3. 3.3 V 稳压输出、电流容量、压差和纹波假设。
4. Teensy、GPS、LIS2MDL、LCD 背光和接口负载的最大估算电流。
5. 每个稳压器输入/输出电容的容量、耐压、ESR 或数据手册要求。
6. VIN/5 V/3.3 V/GND 测试点和故障测量位置。
7. 反接、过流、欠压、掉电和重新上电时的安全状态。

如果器件缺少 SPICE 模型：

- 不填写虚构的电压纹波、温升、峰值电流或稳定性数值；
- 输出电源网络检查、额定值检查和待实物验证清单；
- 把结论标为“规格检查通过，带载性能待实物验证”。

### B. UART/GPS

- 检查 TX/RX 方向、波特率配置假设、共地和电平。
- 模拟有数据、无数据、数据乱码、GPS 无定位和短时失联。
- 说明串口通信正常不等于 GPS 已获得 FIX。

### C. I2C/LIS2MDL

- 检查 SDA/SCL、上拉电阻、电源、共地和地址。
- 检查总线是否可能存在地址冲突、上拉过强/过弱和线长问题。
- 模拟无响应、总线拉低、数据恒定和数据跳变。

### D. SPI/LCD

- 检查 SCK、MOSI、CS、DC、RES、BL 的独立连接。
- 检查 LCD 的 SPI 命名，避免将 `SCL/SDA` 当作 I2C。
- 模拟片选冲突、复位失败、背光正常但数据线无效和刷新过快。

### E. ESC PWM

- 检查 PWM 引脚、信号地、左右映射、上电默认值和中立状态。
- 验证停止、直行、左差速、右差速和 GPS/传感器异常时的安全输出逻辑。
- 只做信号和逻辑验证，不连接真实动力电池和推进器。

## 六、任务 3：PCB 布局、DRC 与制造可用性检查

执行顺序：

1. 确认原理图已通过严格检查，并完成原理图到 PCB 的同步。
2. 读取器件、焊盘、网络、层数、板框和当前 DRC 规则。
3. 检查板框是否闭合，所有器件是否在板框内，安装孔和禁布区是否明确。
4. 检查电源模块、MCU、GPS、LIS2MDL、LCD 接口、ESC 接口的布局顺序和布线通道。
5. 检查去耦电容是否靠近对应电源引脚；不能只看元件存在，还要看实际距离。
6. 检查手焊可达性：默认装配间距至少 40 mil，大焊盘和连接器附近保留约 60 mil 烙铁通道；实际以现场工艺和器件封装为准。
7. 检查连接器朝向、边缘可插拔性、排针编号、线束出口和维护空间。
8. 检查 GPS/RF/天线区域的全层禁布铜，不能只在顶层留空。
9. 检查 GND 铺铜、5 V/3.3 V 电源铜、回流路径和高电流/高噪声区域与磁力计的隔离。
10. 检查信号线宽、供电线宽、过孔尺寸、环宽、铜到板边距离和丝印到焊盘距离。
11. 运行 `pcb layout-lint --gate` 或等效布局门，重点检查重叠、出板框、ratsnest、器件间距和可布性。
12. 运行原生 PCB DRC，并运行 PCB check/DFM，分别记录 ERROR、WARN、INFO 和 skipped 项。
13. 运行网络报告，检查未布线网络、断线、悬空铜、重复线段、过孔重叠、track-over-pad、power-not-poured、width-under-spec、silk-over-pad 和 copper-near-edge。
14. 完成制造输出预览：Gerber、钻孔、BOM、坐标文件和装配方向；没有输出文件时标记为未完成。

规则要求：

- 优先读取当前工程的 live DRC 规则，不直接把通用推荐值当作本板规则。
- 若 live 规则不可读，只能使用嘉立创制造规则作为下限参考，并在报告中写明是 fallback。
- 2 层板电源和地优先使用铺铜；电源主干不能用过细信号线替代。
- 任何 PCB 铜、过孔、铺铜或禁布区修改后，保存并 reload，再重新检查。
- 判定设计正确只看数据检查、DRC、layout-lint、layout-score 和制造输出，不以截图代替。

## 七、任务 4：底板使用场景测试矩阵

至少建立以下场景：

1. 正常输入电压：5 V 和 3.3 V 在规格范围内，主控和外设接口可工作。
2. 输入电压偏低：稳压器进入压差边界，系统进入可识别的欠压/停止状态。
3. GPS 无数据：主控持续运行，不使用无效经纬度进行导航修正。
4. LIS2MDL 无响应：记录故障，不进入自动航向。
5. LCD 未连接：主控和传感器不应因显示模块缺失而产生危险输出。
6. ESC PWM 缺失：左右输出回到安全值，保留急停和人工接管。
7. 传感器线束拔插：不在带电状态插拔；断开后能检测到异常。
8. 调试口占用：串口输出频率不能阻塞 GPS 接收和控制主循环。
9. GND 断开或电平不兼容：应在检查阶段被识别，不能依靠冒烟后才发现。
10. 板框、接口和安装孔与船体安装方案不一致：标记为机械阻塞，不进入打样。

## 八、结论分级

### 仿真通过

同时满足：

- 原理图严格检查通过；
- 原理图与 PCB 网络逐项对账无差异；
- PCB layout-lint 门通过；
- PCB DRC 无 ERROR；
- PCB check/DFM 无阻塞性 ERROR；
- 电源和接口规格检查通过；
- 制造文件可导出并可读；
- 连接器、安装孔和丝印满足装配要求。

### 条件通过

几何、网络和制造检查通过，但存在带载电源、GPS 定位、磁力计干扰、LCD 实物显示或 ESC 信号等只能通过实物验证的项目。

### 待实物验证

需要打样板后验证：

- 实际上电电压和电流；
- 稳压器温升和纹波；
- GPS 定位和天线效果；
- LIS2MDL 航向稳定性和磁干扰；
- LCD 显示和背光；
- ESC PWM 波形和中立值；
- 传感器插拔、急停和异常状态。

### 阻塞

出现以下任一情况：

- 没有原理图/PCB 原生工程；
- EasyEDA 工具或连接器无法连接；
- 原理图和 PCB 网表不一致；
- 有短路、重叠、出板框、未布线关键网或 DRC ERROR；
- 电源额定值、电平、封装引脚或连接器方向无法确认；
- 没有板框、安装孔或关键接口机械约束。

## 九、预期输出文件

今晚至少生成：

1. `PCB底板验证范围-2026-09-10.md`
2. `PCB底板接口网表对账-2026-09-10.csv`
3. `PCB底板电源与接口测试矩阵-2026-09-10.csv`
4. `PCB底板DRC与制造规则检查-2026-09-10.md`
5. `PCB底板上电与联调检查卡-2026-09-10.md`
6. `PCB底板风险清单-2026-09-10.md`
7. `PCB底板仿真与可用性验证报告-2026-09-10.md`

每个结果必须标记数据来源：

- 嘉立创EDA/EasyEDA 原生工程检查；
- SPICE/电气仿真；
- 网表和接口静态检查；
- PCB DRC/DFM；
- 代码分析；
- 待实物验证；
- 阻塞。

没有真实工程或真实仿真输出时，不得填写虚构的 DRC 通过、网络数量、电源波形、温升或底板可用性结论。

## 十、停止条件

满足以下任一条件即可结束今晚夜跑：

1. 底板原理图、PCB 和 Board 绑定关系已确认，且接口网表对账完成。
2. 电源树和 GPS、LIS2MDL、LCD、ESC 接口的电气检查完成。
3. PCB layout-lint、DRC、PCB check/DFM 和制造输出检查完成，所有 ERROR 都有处理结论。
4. 仿真结果与待实物验证项目已分层记录，形成“仿真通过/条件通过/待实物/阻塞”结论。
5. 没有工程、工具或关键规格时，已记录阻塞原因并停止声称底板可用。

## 十一、需要协调与帮助

1. 嘉立创EDA底板工程、原理图和 PCB 文件。
2. 底板的输入电压、5 V/3.3 V 最大电流、层数和板框尺寸。
3. Teensy、GPS、LIS2MDL、LCD、ESC、稳压器、连接器和保护器件的准确型号。
4. 连接器方向、安装孔位置、船体固定方式和传感器离动力系统的距离。
5. EasyEDA Pro 的打开工程、外部交互权限和保存/导出权限。
6. 打样后的限流电源、万用表、示波器/逻辑分析仪、串口工具、急停和固定台架。

## 十二、预期结果与用途

形成一套可审查的嘉立创EDA底板仿真和可用性验证材料，用于：

1. 发现原理图、PCB、网络、封装、引脚和连接器方向错误。
2. 验证电源树、3.3 V/5 V 逻辑、GPS、LIS2MDL、LCD 和 ESC 信号接口是否满足设计假设。
3. 在打样前发现 DRC、DFM、布局、装配、热和机械问题。
4. 明确底板“可以进入打样”“必须修改”“只能等实物验证”的边界。
5. 为后续底板首次上电、传感器联调和推进控制信号测试提供检查卡。

## 十三、记录标识

2026-09-10|张灿|日清
