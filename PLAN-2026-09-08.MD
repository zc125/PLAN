# 2026-09-08 日清夜跑输入材料

## 一、任务背景

本次夜跑只围绕 Gumption-Trap 船体项目复刻，不涉及其他项目。

2026-09-07 已完成 Teensy 4.1、GPS 和 LIS2MDL 磁力计的基础联调：

1. GPS 原始 NMEA 诊断程序已完成编译、烧录和验证。
2. GPS 串口链路正常，正式程序能够持续收到数据，状态为 `gps_recent=1`。
3. 原始数据出现 `ANTENNA OK`，但室内 GPS/北斗可见卫星数均为 `00`，当前 `gps_valid=0`、`sats=0`、经纬度为零。
4. LIS2MDL 状态为 `MAG,OK`，三轴磁场数据和基础航向能够连续输出并随转动变化。
5. 还没有完成 GPS 有效定位、磁力计校准、双 ESC/双推进器测试、自动航向闭环和真实水面测试。

今晚的目标是把“传感器链路已通”推进到“定位、校准、记录和控制逻辑具备可执行验证方法”，不虚构尚未发生的实物测试结果。

## 二、系统范围

本次只分析以下船体复刻系统：

1. Teensy 4.1 主控。
2. GPS 定位模块及 `Serial1` 串口链路。
3. LIS2MDL 磁力计及 I2C 链路。
4. 船体前进方向、磁力计安装方向和软件坐标定义。
5. 左右推进器、双 ESC、供电和急停链路。
6. USB 串口日志、遥测字段和异常记录。

## 三、已有输入材料

- 昨日早班复盘：`/Users/sw-tsj-12/Documents/Codex/2026-08-31/mei/outputs/复盘计划-2026-09-08.md`
- 昨日日清材料：`/Users/sw-tsj-12/Documents/Codex/2026-08-31/mei/outputs/PLAN-2026-09-07.MD`
- 船体项目工作区：`/Users/sw-tsj-12/Documents/Codex/2026-08-31/github-claybuilds-gumption-trap-this-project`
- GitHub 仓库：`https://github.com/zc125/PLAN.git`
- GitHub 目标路径：`daily/2026-09-08/PLAN.MD`
- GPS 诊断程序：`tools/gps_nmea_diagnostic/gps_nmea_diagnostic.ino`
- 正式程序已恢复为 GPS + LIS2MDL 联调版本；所有实际结果必须以今晚保存的串口日志为准。

## 四、今晚给 AI 的执行任务

### 任务 1：制定 GPS 开阔环境验证方案

输出一份可直接执行的 GPS 验证步骤，至少包含：

1. 上电前检查 GPS、Teensy 4.1、共地和串口接线。
2. 将 GPS 天线移动到窗边或室外开阔区域，记录测试地点和开始时间。
3. 连续记录至少 2 分钟原始 NMEA 和正式程序状态。
4. 记录 `gps_recent`、`gps_valid`、卫星数、纬度、经度、时间戳和 NMEA 语句类型。
5. 明确以下判断：
   - 有 NMEA、无卫星、无定位；
   - 有卫星、无定位；
   - 有定位、经纬度不更新；
   - 定位有效且经纬度持续更新。
6. 给出每种情况的下一步动作，不得把“有串口数据”直接判定为“定位成功”。

### 任务 2：制定 LIS2MDL 校准和安装方向验证

输出磁力计校准记录模板，至少包含：

1. 船体前进方向和传感器 X/Y/Z 轴定义。
2. 水平缓慢旋转采样方法。
3. X/Y/Z 最大值、最小值、基础偏移和比例补偿记录。
4. 原始航向、校准航向、安装偏移和航向零点。
5. 判断磁力计无响应、数据恒定、数据跳变、方向相反和受动力系统干扰的方法。
6. 说明磁力计校准未完成前，不得进入自动航向闭环。

### 任务 3：统一遥测字段和日志格式

设计 CSV 或键值日志字段，至少包含：

- `timestamp_ms`
- `gps_recent`
- `gps_valid`
- `sats`
- `lat`
- `lon`
- `gps_track_deg`
- `mag_x`
- `mag_y`
- `mag_z`
- `heading_raw_deg`
- `heading_cal_deg`
- `target_heading_deg`
- `heading_error_deg`
- `left_thruster_cmd`
- `right_thruster_cmd`
- `left_thruster_state`
- `right_thruster_state`
- `battery_voltage`
- `battery_current`
- `fault_code`
- `stop_reason`

同时给出一行示例日志，并明确哪些字段当前可以真实读取，哪些字段需要后续硬件或测试条件支持。

### 任务 4：在不启动推进器的情况下验证控制逻辑

只做逻辑模拟或离线数据验证，不连接真实推进动力，至少覆盖：

1. 停止：左右推进器指令为零，传感器状态仍可读。
2. 直行：左右目标指令相同，检查控制输出格式。
3. 左转：左右指令形成差速，检查软件定义的转向方向。
4. 右转：与左转相反，确认方向约定。
5. 航向修正：计算目标方位与校准航向的误差，并限制修正量。
6. GPS 暂时失效：不使用无效定位数据进行错误修正，进入保持或安全状态。
7. 传感器中断：记录异常状态并保持推进器安全指令。

输出每个场景的输入、预期输出、判断依据和停止条件。模拟结果必须标记为逻辑模拟，不能写成真实船体运动结果。

### 任务 5：制定双 ESC、双推进器低输出测试卡

只准备测试方法，不在没有人工确认和安全条件时执行：

1. ESC 中立状态确认。
2. 单侧低输出、短时间启动。
3. 双侧低输出同速。
4. 低幅度差速转向。
5. 快速换向保护。

每一步明确：

- 开始条件；
- 操作步骤；
- 需要观察的 PWM、转速、电流、电压、温度、振动、噪音和方向；
- 停止条件；
- 急停方式；
- 日志记录内容。

高输出、无固定、无急停、无人工接管时不得进入真实推进测试。

### 任务 6：输出明日行动清单

按三类整理：

1. 必须先做：GPS 开阔环境验证、磁力计安装方向确认、统一日志字段确认。
2. 可以并行：遥测程序字段整理、逻辑场景模拟、低输出测试卡和异常分类。
3. 暂缓：自动航向闭环、航点控制、高输出推进测试、无保护水面测试。

## 五、异常分类和处理边界

至少覆盖：

1. GPS 无数据：检查供电、串口接线、波特率和串口端口。
2. GPS 有 NMEA 但无定位：转移至开阔天空，继续记录，不能使用零经纬度。
3. GPS 有定位但不更新：检查时间戳、天线、信号质量和解析逻辑。
4. 磁力计无响应：检查 I2C 地址、供电、SDA/SCL 和共地。
5. 磁力计数据恒定或跳变：检查读取时序、安装干扰和传感器状态。
6. 航向方向相反：核对坐标轴、安装方向和软件角度定义。
7. 左右推进器方向相反：先断开动力或保持低风险状态，核对 ESC 通道和软件映射。
8. 电池电压、电流或温度异常：立即停止提高输出，保留日志并检查供电安全。
9. 串口日志丢失或时间戳错乱：降低输出频率，检查记录工具和数据格式。

## 六、预期输出文件

今晚至少生成：

1. `GPS开阔环境验证方案-2026-09-08.md`
2. `LIS2MDL校准与安装方向记录-2026-09-08.md`
3. `船体复刻统一遥测字段-2026-09-08.csv`
4. `船体控制逻辑模拟记录-2026-09-08.md`
5. `双ESC双推进器低输出测试卡-2026-09-08.md`
6. `异常分类与明日行动清单-2026-09-09.md`
7. `夜跑结果报告-2026-09-08.md`

所有文件必须标明数据来源。没有真实硬件或真实测试数据时，统一标记为“代码分析”“逻辑模拟”或“待实物验证”。

## 七、停止条件

满足以下任一条件即可结束今晚夜跑：

1. GPS 开阔环境验证步骤和判定标准已经形成。
2. LIS2MDL 校准、安装方向和干扰排查方法已经形成。
3. 遥测字段和日志格式已经统一，并区分已具备字段和待实物字段。
4. 停止、直行、左右转、航向修正和 GPS 失效逻辑已经完成模拟。
5. 双 ESC、双推进器低输出测试卡已经具备开始和停止条件。
6. 遇到无法解释的数据时，已保留原始日志并停止扩大测试范围。

## 八、需要协调与帮助

1. GPS 天线开阔环境和连续供电条件。
2. GPS、LIS2MDL 的准确型号、接线和安装位置。
3. 船体前进方向、磁力计轴向、左右推进器和 ESC 通道定义。
4. 双 ESC、双推进器、电池、保险、急停和固定台架。
5. 真实测试场地、人工接管、紧急断电和船体回收方案。

## 九、预期结果与用途

形成一套可执行、可记录、可审查的船体复刻基础联调方案，用于：

1. 继续确认 GPS 是串口接收正常但无卫星，还是存在供电、接线或解析问题。
2. 完成磁力计校准准备，避免把未校准航向接入自动控制。
3. 统一 GPS、磁力计、推进器指令、电源和异常状态的日志口径。
4. 在真实推进和水面测试前，先通过逻辑模拟和分级测试卡降低风险。

## 十、记录标识

2026-09-08|张灿|日清
