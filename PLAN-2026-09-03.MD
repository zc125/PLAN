# 2026-09-03 日清夜跑输入材料

## 一、任务背景

下午已经生成“太阳能自主船推进仿真工作台”前端界面。今晚不再从空白方案开始，而是直接使用这个界面模拟双 T200 船体下水后可能遇到的运行状态，利用前端的实时可视化、风险提示、运行记录和 CSV 导出能力，生成一组可追溯的多工况仿真数据。

本次工作的目标是：

1. 验证前端仿真链路是否完整可用。
2. 用统一的记录方式比较不同水流、阻力、供电和控制条件。
3. 找出值得优先做真实水槽测试的工况。
4. 提前暴露掉压、过流、碰壁、回流、转向不稳、换向冲击和自动航向修正等问题。

所有输出必须标注为“前端模型仿真数据”，不能写成真实下水实测结果。

## 二、前端地址与已具备能力

前端文件：

`/Users/sw-tsj-12/Documents/Codex/2026-08-31/github-claybuilds-gumption-trap-this-project/仿真工作台/太阳能自主船推进仿真工作台.html`

已具备的主要功能：

- 可调水缸长度、宽度、水深、水流速度和水温。
- 可调船体质量、阻力系数和推进器间距。
- 可调电池标称电压、等效内阻、逻辑侧电流、PWM 变化限速、电机响应时间和仿真倍率。
- 双 T200 左右目标 PWM 与实际 PWM 显示。
- 双机同速、左转、右转、双机停止等预设。
- 目标航向、自动回正/航向保持和锁定当前航向。
- 船体速度、航向变化率、电池端电压、总电流实时显示。
- 推力、水阻、回流、船体合力和差速转向计算链路显示。
- 轨迹、船体姿态、水流方向、风险提示和最近运行记录可视化。
- 运行记录 CSV 导出。

## 三、今晚输入给 AI 的任务

请按以下顺序操作或指导操作前端，并对每组工况单独导出 CSV。

### 任务 1：确认基准工况

1. 打开前端 HTML。
2. 点击“重置”，确认页面回到初始状态。
3. 保持默认参数：
   - 水缸：3.0 m × 1.2 m × 0.6 m
   - 水流：0 m/s
   - 水温：24 °C
   - 船体质量：12 kg
   - 船体阻力系数：1.2
   - 推进器间距：0.65 m
   - 电池：12.8 V，等效内阻 0.035 Ω
   - PWM 变化限速：90 μs/s
   - 电机响应时间：0.9 s
3. 点击“双机停止”确认初始状态。
4. 点击“同速前进”，运行至少 30 秒。
5. 记录页面上的仿真时间、速度、航向变化率、电压、总电流、左右 PWM、风险提示和轨迹。
6. 暂停后点击“导出 CSV”，保存为：
   `01_基准_双机同速前进.csv`

判断重点：页面是否持续运行，数据表是否增加，实时曲线是否更新，CSV 是否能够打开且包含时间、电压、电流、速度、推力、航向和风险字段。

### 任务 2：模拟同向水流和逆向水流

每次开始前都点击“重置”，避免上一组轨迹和数据混入。

工况 A：同向水流

- 水流速度：`+0.50 m/s`
- 其他参数恢复默认
- 双机同速前进
- 运行 30 秒
- 导出：`02_同向水流_双机前进.csv`

工况 B：逆向水流

- 水流速度：`-0.50 m/s`
- 其他参数恢复默认
- 双机同速前进
- 运行 30 秒
- 导出：`03_逆向水流_双机前进.csv`

判断重点：比较两组的船体速度、世界坐标轨迹、方向标识、总电流和碰壁状态。说明前端的水流速度是模型输入，不能直接等同于真实水域流速测量。

### 任务 3：模拟差速转向

工况 A：左侧推力较大：

- 点击“重置”
- 左侧目标 PWM：`1630 μs`
- 右侧目标 PWM：`1550 μs`
- 自动航向控制先关闭
- 运行 30 秒
- 导出：`04_左高右低_差速转向.csv`

工况 B：右侧推力较大：

- 点击“重置”
- 左侧目标 PWM：`1550 μs`
- 右侧目标 PWM：`1630 μs`
- 自动航向控制先关闭
- 运行 30 秒
- 导出：`05_左低右高_差速转向.csv`

判断重点：确认页面上的转向方向文字、航向变化率、左右推力差、左右转速差和轨迹是否相互一致；如果方向理解与前端提示不一致，记录为“需核对推进器实际安装方向”，不要直接修改结论。

### 任务 4：模拟高阻力和小容积回流

工况 A：高船体阻力：

- 点击“重置”
- 船体阻力系数：`3.0`
- 其他参数默认
- 双机同速前进
- 运行 30 秒
- 导出：`06_高阻力_双机前进.csv`

工况 B：小水缸：

- 点击“重置”
- 水缸长度：`1.8 m`
- 水缸宽度：`0.8 m`
- 水深：`0.4 m`
- 其他参数默认
- 双机同速前进
- 运行至出现“船体已接近水缸边界”或最多 30 秒
- 导出：`07_小水缸回流与碰壁.csv`

判断重点：区分“阻力上升”与“容器边界/回流影响”。碰壁后的速度被模型归零，碰壁数据不能用于推断真实船速。

### 任务 5：模拟低电源余量和风险工况

工况 A：较高内阻：

- 点击“重置”
- 电池标称电压：`12.0 V`
- 电池等效内阻：`0.10 Ω`
- 双机同速前进
- 运行 30 秒
- 导出：`08_高内阻_掉压趋势.csv`

工况 B：较低标称电压：

- 点击“重置”
- 电池标称电压：`10.8 V`
- 电池等效内阻：`0.05 Ω`
- 双机同速前进
- 运行 30 秒或出现控制器掉压风险
- 导出：`09_低电压_控制风险.csv`

工况 C：高输出风险演示：

- 只在前端模型中运行，不连接真实 T200、ESC 或电池。
- 点击“重置”
- 电池标称电压：`12.0 V`
- 电池等效内阻：`0.12 Ω`
- 左右目标 PWM：`1900 / 1900 μs`
- 运行至出现过流或掉压风险，最长 10 秒
- 导出：`10_高输出_过流掉压风险.csv`

判断重点：记录风险提示出现的时间、最低电压、最大电流和风险字段。风险工况只用于说明安全边界，不是实际硬件测试建议。

### 任务 6：模拟快速换向保护

1. 点击“重置”。
2. 关闭自动航向控制。
3. 点击“同速前进”，运行 10 秒。
4. 将左右目标 PWM 同时改为 `1400 μs`。
5. 观察实际 PWM 是否先回到 1500 μs，再经过约 0.35 秒允许反向建立推力。
6. 运行 15 秒后导出：
   `11_快速换向_保护状态.csv`

判断重点：记录目标 PWM 与实际 PWM 的差异、换向保护状态、总电流和速度变化，确认模型没有把目标指令瞬间当成实际推进器输出。

### 任务 7：模拟自动航向保持

1. 点击“重置”。
2. 保持默认参数，点击“右转”或手动设置左右不同 PWM，先制造约 30° 航向变化。
3. 设置目标航向为 `0°`，开启“自动回正与航向保持”。
4. 点击“设为目标”，继续运行 30 秒。
5. 导出：
   `12_自动航向保持_误差收敛.csv`

判断重点：比较航向角、目标航向、航向误差、航向变化率、左右目标 PWM 和实际 PWM。重点观察是否存在持续振荡、修正过猛或误差不收敛。

## 四、每组数据必须记录的内容

每个导出文件旁边建立一条工况说明，至少包括：

- 文件名。
- 工况编号和名称。
- 开始前的全部参数。
- 运行时长和实际导出数据条数。
- 最大总电流。
- 最低电池端电压。
- 最大/最终船体速度。
- 最大航向变化率。
- 最大左右推力差。
- 是否出现碰壁、换向保护、掉压、过流或高温风险提示。
- 是否适合进入真实水槽低输出验证。
- 仿真与真实下水之间的差异和限制。

如果前端当前只能逐组导出，不要虚构“批量导出”能力；逐组导出后再通过汇总表进行比较。

## 五、数据分析要求

请将导出的数据按以下三类归档：

### A 类：可作为低风险实物验证起点

- 默认参数下的双机低输出同速。
- 单变量水流干扰。
- 低幅度差速转向。
- 快速换向保护确认。

### B 类：必须先补充实物条件

- 高阻力。
- 小水缸回流和碰壁。
- 自动航向保持。
- 高内阻或低电压供电。

### C 类：禁止直接在真实硬件上复现

- 高输出过流/掉压风险演示。
- 任何页面显示严重风险且原因未确认的组合。
- 未确认推进器方向、急停和供电保护时的快速反向测试。

## 六、异常排查顺序

如果出现异常，按以下顺序检查：

1. 是否忘记重置，导致上一组数据或轨迹未清空。
2. 当前工况参数是否已正确填写，是否存在多个变量同时修改。
3. 目标 PWM 与实际 PWM 是否一致，是否仍处于平滑过渡或换向保护。
4. 风险列表具体提示是什么，首次出现在哪个仿真时间点。
5. 电压、电流、速度、推力和航向数据是否同时异常。
6. 是否是水缸边界、回流或水流输入导致的模型现象。
7. 是否达到停止条件；达到后立即暂停并导出当前数据。
8. 将该组标记为“需人工复核”，不使用未经解释的数据下结论。

## 七、输出文件

今晚至少形成：

1. 6-12 份按工况命名的原始 CSV。
2. 1 份工况汇总表，列出参数、关键峰值、风险和结论。
3. 1 份异常问题清单，列出“现象、时间点、参数、可能原因、下一步排查”。
4. 1 份前端可用性记录，说明运行、可视化、暂停、重置和导出是否正常。

## 八、约束条件

- 所有数据标记为“前端模型仿真”，不得称为真实下水数据。
- 每次只改变一个主要变量，避免无法归因。
- 每组开始前重置，结束后立即导出并记录文件名。
- 高输出风险工况只在前端运行，不连接真实动力设备。
- 不根据仿真数据直接确定真实 T200 的最终电源、保险丝、线径或 PID 参数。
- 不用模型温度、噪音、振动和密封字段替代真实测量。
- 页面出现明显风险、数据失真或逻辑无法解释时，立即停止该工况。

## 九、停止条件

满足以下任一条件即可结束今晚夜跑：

1. 基准工况和主要异常工况均已完成导出，并形成汇总表。
2. 前端运行、可视化或 CSV 导出出现阻塞，已记录具体错误和复现步骤。
3. 出现过流、掉压、数据异常或页面无法解释的状态，已保留现场数据并停止继续加大输出。
4. 已经得到下一步真实水槽测试的前三项优先验证内容。

## 十、预期结果与用途

今晚应得到一套基于仿真前端的双 T200 多工况数据，至少覆盖：

- 基准同速前进。
- 同向/逆向水流。
- 左右差速转向。
- 高阻力和小水缸回流/碰壁。
- 高内阻、低电压和高输出风险。
- 快速换向保护。
- 自动航向保持。

这些数据用于：

1. 验证前端界面的可视化和导出能力。
2. 观察参数变化对电流、电压、速度、推力、轨迹和航向的影响。
3. 确定真实水槽测试的优先级和停止线。
4. 提前整理需要实物测量和人工复核的问题。

## 十一、记录标识

2026-09-03|张灿|日清
